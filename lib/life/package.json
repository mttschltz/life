{
  "name": "life",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "yarn lint:types && yarn lint:eslint",
    "lint:eslint": "eslint '**/*.{js,ts,tsx}' --max-warnings 0",
    "lint:types": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23"
  }
}
